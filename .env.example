NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_WLV_WIDGET_KEY=
NEXT_PUBLIC_WLV_CONTAINER_ID=
LOUPE_API_BASE_URL=https://api.loupe.immo
LOUPE_API_EMAIL=
LOUPE_API_PASSWORD=
PUBLIC_SITE_URL=
RESEND_API_KEY=
EMAIL_FROM_EMAIL=
EMAIL_FROM_NAME=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME=
SUPABASE_SERVICE_ROLE_KEY=
ADMIN_API_KEY=
OPENAI_API_KEY=
DOMAIN_EVENTS_CRON_SECRET=
SWEEPBRIGHT_MEDIA_BUCKET=property-media-cache
# Shared secret used to authenticate POSTs from the Zapier "SweepBright"
# Zaps to /api/webhooks/sweepbright-zapier (header X-Zapier-Secret).
# Generate with `openssl rand -hex 32`. Rotate annually or whenever the
# value has been exposed (chat, screenshot, browser inspect, etc.).
SWEEPBRIGHT_ZAPIER_WEBHOOK_SECRET=
# Google Tag Manager. Set ONLY on the production environment (e.g. GTM-WCCJQJN2).
# Leave empty on local / preview to avoid polluting analytics with non-prod traffic.
NEXT_PUBLIC_GTM_ID=
# HMAC secret for buyer alert unsubscribe links. REQUIRED in production
# (a dev-only fallback is used locally). Generate with `openssl rand -hex 32`.
BUYER_ALERT_HMAC_SECRET=
# 32+ random chars signing the anonymous AI session cookie. REQUIRED in
# production (dev-only fallback locally). Generate with `openssl rand -hex 32`.
SILLAGE_AI_SESSION_SECRET=
# HMAC secret for the 24h "merci vendeur" access tokens. REQUIRED in
# production (dev-only fallback locally). Generate with `openssl rand -hex 32`.
MERCI_VENDEUR_ACCESS_SECRET=
